# Handoff: добить пакетные задачи productize-pi-1c-agent (4.4, 9.1–9.5)

**When**: 2026-09-14 22:36
**Branch / commit**: `main`, latest commit `cef2e10 chore: init pi-1c-agent profile repo (rules, agents, skills, prompts, settings)` — APPLY профиля ещё не закоммичен.
**Next session focus**: пакет `pi-1c-agent` — регистрация `/init`/`/doctor`, TUI, bootstrap MCP, `doctor.mjs`, docker-блок в `1c-mode`.

## Current State

APPLY OpenSpec-изменения `productize-pi-1c-agent` в репозитории профиля Pi 1C сделан для файлов профиля: **56/62** задач. Канонические команды без префикса `1c-`, пустой default `mcp.json`, Docker allow+confirm+degrade, CAVEMAN empty=`auto`, `/review-airules` + пин Comol. Остались только задачи пакета `pi-1c-agent` (другой git tree). Пользователь спросил, почему они не сделаны: design decision 8 + `allowedEditRoots` = этот профиль.

## Open Questions

- Делать ли 4.4 и 9.1–9.5 в дереве пакета в следующей сессии, или архивировать изменение профиля и вести пакет отдельно.
- Коммит APPLY профиля пользователь не просил — незакоммиченные правки лежат в рабочем дереве.

## Files Changed In This Session

Профиль (не дублировать содержимое — смотреть git status / OpenSpec):

- `prompts/` — rename без `1c-`, алиасы `1c-*.md`, `CATALOG.md`, `commands.md`, `init.md`, `doctor.md`, `doctor-explain.md`, `review-airules.md`, `install-openviking.md`
- `mcp.json`, `mcp.optional/`, `mcp.example.json`
- `AGENTS.md`, `README.md`, `NOTICE`, `settings.json`, `trust.example.json`
- `agents/*.md`, `skills/` (caveman, mcp-1c-tools, context-bootstrap, shared-memory, session-handoff)
- `rules-1c/` (пути `$PI_CODING_AGENT_DIR`, handoff JSON, CAVEMAN `auto`)
- `upstream.lock.json`, `UPSTREAM-REGISTER.md`
- `openspec/changes/productize-pi-1c-agent/tasks.md`, `verification.md`, `package-followup.md`

## Verification State

`openspec/changes/productize-pi-1c-agent/verification.md`, итерация 1: обязательные строки профиля **pass**. 4.4 и 9.1–9.5 **skip** (вне этого git tree). `docker ps` в Cursor сработал (allow). Живую ИБ не грузили, Comol не импортировали, бандл MCP не ставили.

## Next Steps

1. Открыть дерево пакета `pi-1c-agent` (типично `~/.pi/packages/pi-1c-agent`, не этот репозиторий).
2. Сделать задачи из `openspec/changes/productize-pi-1c-agent/package-followup.md`: `registerCommand("init"|"doctor")` + алиасы `/1c-*`; TUI `/init` вопрос 0; bootstrap не пишет memory/knowledge/8002–8008; `doctor.mjs` CORE на чужие пути и лишний MCP; `1c-mode` docker-блок только при `PI_1C_BLOCK_DOCKER=1` или нет сокета.
3. После пакета: `/opsx-apply productize-pi-1c-agent`, отметить 4.4 и 9.1–9.5, при необходимости дописать `verification.md`.
4. Коммит профиля — только если пользователь явно попросит.
5. Не архивировать change, пока пакетные задачи открыты, если цель — одно зелёное APPLY.

## What To Load Next Session

- **Slash commands**: `/opsx-apply productize-pi-1c-agent` после правок пакета.
- **On-demand**: `openspec/changes/productize-pi-1c-agent/design.md` (таблица APPLY vs package), `package-followup.md`, `tasks.md` §9.
- **MCP tools**: не обязательны для пакета (это TypeScript/mjs, не 1C dump).
- **Subagents**: не нужны, пока работа в одном дереве пакета.

## Links (DO NOT copy content)

- `openspec/changes/productize-pi-1c-agent/proposal.md`
- `openspec/changes/productize-pi-1c-agent/design.md` — decision 8
- `openspec/changes/productize-pi-1c-agent/tasks.md` — открыты 4.4, 9.1–9.5
- `openspec/changes/productize-pi-1c-agent/verification.md`
- `openspec/changes/productize-pi-1c-agent/package-followup.md`
- `upstream.lock.json` — SHA `410951e74fd3e6b7a763cf49757935b9a34d3f31`
- `NOTICE`, `prompts/CATALOG.md`
