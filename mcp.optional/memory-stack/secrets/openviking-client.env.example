# OpenViking client (user) key. Copy to openviking-client.env (gitignored).
# Leave empty: scripts/memory-stack.sh provisions one after OpenViking is healthy.
OPENVIKING_CLIENT_API_KEY=
OPENVIKING_CLIENT_ACCOUNT_ID=mcp_workspace
OPENVIKING_CLIENT_USER_ID=agent
